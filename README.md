# Aelora Virtual Gateway

This is the separately runnable Python virtual solar-site gateway for Aelora. It is intentionally a sibling project, not a package or route inside the Next.js application.

Implementation is being developed test-first. Full setup and operating instructions are added with the working gateway.
